# Finales de linea normalizados. Sin esto, Git en Windows convierte todo a
# CRLF y los scripts de shell (como gradlew) dejan de andar en Linux y Mac.
* text=auto
gradlew text eol=lf
*.sh text eol=lf
*.bat text eol=crlf
*.png binary
*.jar binary
